// redis 접속 정보
export const host = '';              // redis ip
export const port = 0;               // redis port
export const db = 0;                 // redis db
export const password = undefined;   // redis password (없으면 생략 가능)